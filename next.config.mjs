/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
      domains: ['parthsarthibuilder.com', 'res.cloudinary.com'], // Added Cloudinary domain here
  },
};

export default nextConfig;
